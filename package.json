{
  "name": "feedbacks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.18.0",
    "json-server": "^0.14.2",
    "prop-types": "^15.7.2",
    "react": "^16.8.4",
    "react-dom": "^16.8.4",
    "react-icons": "^3.5.0",
    "react-scripts": "2.1.8",
    "react-timeago": "^4.4.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "dev": "react-scripts build && rimraf dev && parcel build/index.html -d dev --port 3003",
    "build-emb": "react-scripts build && rimraf dist && parcel build --no-cache --no-minify build/index.html",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "json-server --watch json/feedbacks.json --port 3004"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "parcel-bundler": "^1.12.2"
  }
}
